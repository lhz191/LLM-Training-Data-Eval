#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
xLAM 常量定义
"""

# =============================================================================
# LLM 配置（用于 LLM Judge）
# =============================================================================

LLM_API_KEY = 'sk-o0QqcwC8XNHU6gGT7CYdMSQGJQQMtjKJSqw6K9G21IaoOElt'
LLM_BASE_URL = 'http://35.220.164.252:3888/v1/'
LLM_MODEL = 'gpt-4.1'


# =============================================================================
# LLM Judge Prompt 模板
# =============================================================================

TOOL_SELECTION_PROMPT = """你是一个 Agent 数据质量评估专家。请判断以下 API 调用是否能满足用户的需求。

【用户查询】
{query}

【API 调用】
{api_calls}

请判断：这些 API 调用是否能满足用户的实际需求？

判断标准：
- API 调用应该能够完成用户请求的任务
- 仅仅"相关"是不够的，必须能够"解决问题"
- 参数填充应该正确反映用户的意图
- 例如：用户要"预订机票"，调用"查询航班"是不够的，因为没有完成预订

请以 JSON 格式输出：
```json
{{
    "satisfied": true/false,
    "reason": "简要说明判断理由"
}}
```

只输出 JSON，不要其他内容。"""
